#!/bin/bash
# 文字数カウント（ファイルまたは標準入力）

# 入力がない場合は使い方を表示
if [ $# -eq 0 ] && [ -t 0 ]; then
    echo "Usage: $0 [file] or pipe input" >&2
    exit 1
fi

# ファイルが指定されている場合
if [ $# -ge 1 ]; then
    wc -m < "$1"
else
    # パイプ入力の場合
    wc -m
fi


